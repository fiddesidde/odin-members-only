<% layout('layouts/boilerplate') %>
<h1>Welcome to a members only message board</h1>
<% if (!currentUser) { %>
    <div>Sign up or login over there...</div>

<% } else if (currentUser) {%>
    <div>Congrats, you're in.</div>
<% } %>
<% for( message of messages ) { %>
    <div class="card mb-3">
        <div class="row">
            <div class="col-md">
                <div class="card-body">
                    <a href="/messages/<%= message._id %>">
                        <h5 class="card-title">
                            <%= message.title %>
                        </h5>
                        <div class="card-text text-muted">
                            <%= message.author.username %>
                        </div>
                        <div class="card-text">
                            <%= message.content %>
                        </div>
                    </a>
                </div>
            </div>
        </div>
    </div>
 <% } %>